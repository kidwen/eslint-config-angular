import { Routes } from '@angular/router';
import { HomeComponent } from 'src/tank/home/home.component';

export const APP_ROUTES: Routes = [
    {
        path: '',
        component: HomeComponent,
    },
];
